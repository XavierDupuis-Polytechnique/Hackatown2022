export interface Seller {
    _id: string;
    name: string;
    userId: string;
    description: string;
    imageUrl: string;
    reviews: string;
}

export interface SellerIdentity {
    id: string;
}
